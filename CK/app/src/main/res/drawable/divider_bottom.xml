<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" />  <!-- Nền trong suốt, hoặc thay bằng #FFFFFF nếu muốn trắng -->
    <stroke
        android:width="1dp"
        android:color="#000000" />  <!-- Viền đen, rộng 1dp -->
    <corners android:radius="8dp" />  <!-- Góc bo tròn 8dp, bỏ nếu không muốn -->
</shape>